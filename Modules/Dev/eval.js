String.prototype.json = function() {
				 return JSON.parse(fs.readFileSync(this.toString() + '.json').toString())
				}
if('./Arrays/owners'.json().hasOwnProperty(message.author.id.toString())) {
	const os = require("os")
	try{
	if(message.content.replace(arg[0] + ' ', '') != undefined) eval(message.content.replace(arg[0] + ' ', ''))
	}catch(err){message.channel.send(`\`\`\`${err.toString()}\`\`\``)}
	}